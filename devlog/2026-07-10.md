# 2026-07-10 开发日志

## 今日完成

### 项目规范建设
- [x] 创建 `docs/` 文件夹：需求文档、架构设计、编码规范、执行计划、API参考
- [x] 创建 `devlog/` 文件夹：开发日志模板 + 今日日志
- [x] 创建 `CLAUDE.md`：AI 助手指引，指向所有标准文件

### Phase 1-5 代码完成（之前已完成）
已创建全部模块代码，详见 [implementation-plan.md](../docs/implementation-plan.md) 中标记为 ✅ 的条目。

### Step 6.1 环境验证 ✅
- [x] `pip install -e .` — 全部依赖安装成功（34个包）
- [x] `playwright install chromium` — Chromium 浏览器安装成功
- [x] 全部 21 个模块导入验证通过
- [x] CLI `--help` 可用，6 个命令正常注册
- [x] 核心工具函数验证通过：`parse_chinese_number`（万/亿/k/w）、`safe_int`、`safe_date`

### CSV 采集器验证 ✅
- [x] 创建示例 CSV 测试数据（包含中文数字：万/k/亿，汇总行+笔记行）
- [x] `CSVImportCollector.collect_all()` 完整采集流程通过
- [x] `normalize_collect_result()` 标准化流程通过
- [x] 全部 13 项单元验证 + 7 项集成验证通过

### CSV → Normalize → SQLite Pipeline ✅
- [x] 端到端链路：CollectResult → AccountSnapshot/NoteInfo/NoteSnapshot → SQLite 落库
- [x] 数据回读验证：所有字段值与原始 CSV 数据一致
- [x] SyncState 状态记录正常

### 线路 A：Trend + Competitor + Pipeline 离线模式 ✅
- [x] **TrendCalculator** — 13/13 通过
  - DoD 日环比增量/增长率计算正确（+60 粉丝/天，0.2%）
  - WoW 周环比增量/增长率计算正确（+420 粉丝/周，1.2%）
  - 3σ 异常检测：异常值正确触发（z≈4.0 > 3.0），正常值不误报
  - 笔记趋势：views DoD +14000，best_growing_metric 识别正确
  - 边界情况：无历史数据时优雅降级（DoD=current，无异常标记）
- [x] **CompetitorAnalyzer** — 13/13 通过
  - 按粉丝数排名正确（competitor_a > main_brand > competitor_b）
  - build_comparison：排名/互动率/笔记数/平均互动量全部正确
  - enrich_with_deltas：DoD=+60/+20/+45，WoW=+420/+140/+315 均正确
  - rank_change/rank_changes_summary：首次对比全部标记为"新"
- [x] **SyncEngine 离线模式** — 7/7 通过
  - 无飞书凭证时自动进入离线模式（enabled=False）
  - 全部 5 个 sync 方法返回 0 不抛异常
- [x] **端到端集成** — 5/5 通过
  - 3 个账号 × 完整处理链路：采集→存储→趋势→竞品→离线同步
  - 全部不抛异常，离线同步优雅跳过

### 修复的问题（累计 8 个）
1. [x] **`bitable_client.py`**: 移除两个不存在的 import 类 — lark-oapi v1.7.1 API 变更
2. [x] **`feishu_bot.py:184`**: 修复 docstring 损坏的引号字符
3. [x] **`csv_import.py`**: 移除重复的 `_parse_chinese_number`，统一复用 normalizer 版本
4. [x] **`csv_import.py`**: `collect_account_profile` 补全四字段提取（原来只提取粉丝数）
5. [x] **`sync_engine.py`**: 添加 `enabled` 属性 — 飞书未配置时自动离线模式
6. [x] **`sync_engine.py`**: 异常捕获扩展到 `XHSFeishuSyncError`（覆盖 `MissingCredentialError`）
7. [x] **`trend_calculator.py`**: `_calculate_trend` 添加 `or 0` 保护 — 空对象属性为 None 时不再崩
8. [x] **`verify_trend_competitor.py`**: NoteSnapshot 创建后补上 `ns_repo.save()` 调用

### 项目整体状态
- **代码文件**: ~33 个源文件
- **已验证模块 (18/21)**: config, exceptions, logging, normalizer, csv_import, models (ORM), sqlite, trend_calculator, competitor, sync_engine (offline), pipeline (offline), xhs_api, xhs_browser, factory, base, bitable_schema, cli, collector_models
- **待验证模块 (3/21)**: bitable_client, sync_engine (online), feishu_bot, scheduler, pipeline (full with Feishu)

## 待办事项（按优先级）

### 高优先级 — 飞书集成验证
- [ ] 配置 .env（需要真实飞书凭证：FEISHU_APP_ID、FEISHU_APP_SECRET、FEISHU_BITABLE_APP_TOKEN）
- [ ] `xhs-feishu setup` 初始化数据库 + 飞书表
- [ ] `xhs-feishu test-feishu` 连接测试
- [ ] CSV → SQLite → Trend → Competitor → Feishu Bitable 端到端

### 低优先级 — 浏览器采集验证
- [ ] Chrome CDP 模式启动脚本
- [ ] `xhs-feishu test-collect` Browser 模式干跑

## 遇到的问题
1. **lark-oapi 版本 API 变更**: v1.7.1 移除了 2 个 import 类，已修复 ✅
2. **feishu_bot.py 编码损坏**: docstring 引号字符损坏，已修复 ✅
3. **csv_import 代码重复**: 维护了两套 `parse_chinese_number`，已统一到 normalizer ✅
4. **csv_import profile 提取不全**: 只提取粉丝数，已补全 ✅
5. **PowerShell 编码**: 中文输出乱码（不影响代码，仅显示问题）
6. **NoteSnapshot 未 save**: 验证脚本中 NoteSnapshot 创建后未调用 save，已修复 ✅
7. **AccountSnapshot() 空对象**: ORM default 只在 INSERT 时生效，手动创建属性为 None，trend_calculator 已加 `or 0` 保护 ✅
8. **SyncEngine 异常捕获不足**: `load_config()` 在 BitableClient 初始化前就抛 `MissingCredentialError`，已扩展到 `XHSFeishuSyncError` ✅

## 下一步建议
只剩最后一块：**飞书集成**。需要准备飞书应用凭证，之后 `xhs-feishu run` 即可端到端打通。
