# 2026-07-13 开发日志

## 今日完成

### 线路 B：飞书集成 ✅ (端到端打通)

飞书写入权限 + 表结构 + API 兼容性全部解决，`xhs-feishu run` 端到端跑通。

#### 解决的问题（#13 ~ #15）

13. [x] **飞书 91403 Forbidden（写入权限）** — 自建应用未获管理员授权。用户通过在多维表格中「添加文档应用」替代管理员安装流程，解决了文档级写权限。
14. [x] **建表 API `default_view_name` 参数 1254001** — 该 Bitable 版本不支持此参数，改为仅传 `name`。
15. [x] **建表后字段为空** — `create_table` 只建空表不加字段。新增 `BitableClient.add_field()` / `_list_fields()` 方法，`BitableSchemaManager.setup_all_tables()` 现在自动创建 schema 中定义的所有字段（幂等）。
16. [x] **DateTime 字段格式 1254064** — 飞书 DateTime 字段需要毫秒时间戳，之前传的是格式化日期字符串。新增 `_to_timestamp()` 辅助函数，所有 DateTime 字段统一转换。

#### 端到端验证结果

| 步骤 | 结果 |
|------|------|
| `xhs-feishu setup` 建表+加字段 | ✅ 4 表 51 字段全部创建 |
| `xhs-feishu test-feishu` | ✅ Token 获取 + 8 表列表 |
| `xhs-feishu run` 端到端 | ✅ Pipeline 完整跑通 |
| 账号概览表数据 | ✅ test_brand 数据正确写入 |
| 每日快照表数据 | ✅ 快照行正确 |
| 幂等性（二次 run） | ✅ created=0, updated=1 |
| 笔记明细表 | ✅ 0 条（CSV 无笔记数据，正确）|

#### 代码变更清单
- `src/loaders/bitable_client.py`: 
  - `create_table()` 移除必传 `default_view_name`
  - 新增 `add_field()`, `_list_fields()` 方法
- `src/loaders/bitable_schema.py`: `setup_all_tables()` 增加字段自动创建
- `src/loaders/sync_engine.py`: 新增 `_to_timestamp()`, 所有 DateTime 字段改用毫秒时间戳

### Phase 5 调度器验证 ✅ (15/15)
- [x] **Check 1: 调度器创建和配置** — 5/5 通过
  - `create_scheduler()` 返回 BackgroundScheduler
  - 状态正确 (STATE_STOPPED)
  - 'daily_xhs_sync' 任务已注册，名称/参数正确
- [x] **Check 2: 简单任务触发** — 3/3 通过
  - APScheduler 能正常 start
  - 0.5 秒间隔定时任务正确触发 2 次以上
  - 触发间隔准确
- [x] **Check 3: 错误监听器** — 1/1 通过
  - `_job_listener` 处理 EVENT_JOB_ERROR 不抛异常
  - `_run_sync_job()` 的 try-except 能捕获 pipeline 异常
- [x] **Check 4: `_run_sync_job()` 离线模式** — 1/1 通过
  - 离线模式下不崩溃，Pipeline 正常运行
  - SyncEngine 自动进入离线模式（enabled=False）
  - Bot 通知自动跳过（webhook 未配置时 enabled=False）
- [x] **Check 5: SyncState 状态记录** — 3/3 通过
  - 任务执行后 SyncState 正确写入 SQLite
  - CLI status 可读取 SyncState
  - 状态为 success
- [x] **Check 6: 优雅关闭** — 2/2 通过
  - shutdown() 成功，状态从 STATE_RUNNING → STATE_STOPPED
  - shutdown 后调度器干净

### 修复的问题（累计 12 个）
9. [x] **`config.py` feishu validator 过严** — 无飞书凭证时 `load_config()` 直接抛异常，离线模式无法工作。改为允许空值，由下游 BitableClient/SyncEngine 处理
10. [x] **`config.py` accounts validator 缺少 None 保护** — YAML 空列表被解析为 None，pydantic 校验失败。添加 `field_validator` 将 None 转为 `[]`
11. [x] **`main.py` CLI 中文输出 GBK 编码** — 添加 Windows UTF-8 stdout/stderr wrapper
12. [x] **`accounts.yaml` 空列表语法** — `competitor_accounts` 无条目时注释掉导致 None。改为 `competitor_accounts: []`

### 配置变更
- `config/settings.yaml`: 默认策略 `browser` → `csv`（反映当前架构决策）
- `config/accounts.yaml`: 启用 `test_brand` 测试账号，`competitor_accounts` 使用空列表语法
- `config/accounts.yaml`: `competitor_accounts` 使用空列表语法 `[]`

## 项目当前状态

```
Phase 1: 基础框架           ✅ 已验证
Phase 2: 数据采集层          ⚠️ CSV已验证 / Browser未测试
Phase 3: 数据转换层          ✅ 已验证 (26/26)
Phase 4: 飞书同步            ✅ 在线已验证 (端到端跑通)  ← 本次完成
Phase 5: 调度与通知          ✅ 已验证 (15/15)
Phase 6: 集成测试            ✅ 线路A+线路B 全部通过     ← 本次完成
```

- **总验证数**: 53 (Line A) + 7 (Line B) = 60 项全部通过
- **已修复问题**: 16 个
- **阻塞项**: ~~飞书凭证~~ 已解决 ✅

## 待办事项

### 中优先级 — 融合新方案优点
- [ ] 编写 `scripts/xhs_login.py` 扫码登录脚本
- [ ] `xhs_browser.py` 增加 storageState 启动模式
- [ ] 创建 `.github/workflows/sync.yml`

### 低优先级
- [ ] Browser CDP 采集验证
- [ ] Bot 通知端到端验证
- [ ] 每日快照去重（同天重复 run 会产生重复行）
- [ ] README 编写
